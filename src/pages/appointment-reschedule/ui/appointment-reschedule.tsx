import { PageContainer } from '@/shared/ui';

import styles from './appointment-reschedule.module.scss';

export const AppointmentReschedulePage = () => {
  return (
    <PageContainer>
      <div className={styles.root}>
        <p>Appointment Reschedule Page</p>
      </div>
    </PageContainer>
  );
};
